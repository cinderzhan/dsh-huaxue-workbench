# 验证范围

候选版本：0.3.0-rc.1。验证日期：2026-09-16。目标：Windows / DSH Desktop 0.9.0 / Harness 0.1.5-rc.2。

## 已完成

- 六个宿主原生文件 SHA-256 与升级后的原始备份一致；新插件不修改宿主源码。
- 空白配置安装：入口、七人页、工作区选择、选人聊天、切人、游戏目录、情境选择与接话提交；无需创建或选择 huashao2 预设。
- 用户实际 Harness 的真实模型验收：静静子首轮、同会话切换晴公主后的第二轮，以及 S08 酒店找人游戏续演和结束记录。两个会话均为 standard，三轮均完成且无错误事件。
- 新版 system/message 记录分别包含角色 ning、qing 与游戏 S08 标记；不以口吻相似代替规则注入证据。
- 实际 DSH 桌面侧栏入口与七人选择页通过；Harness 完整重启后正常加载插件。
- 24 项包级测试、原生 Harness 服务集成测试、构建、可移植性检查和 npm pack 通过。
- 真实干净安装、重复安装、手动回退后组合配置验证通过；人为设置无效本地依赖触发 pnpm 失败后，原配置与依赖目录恢复。未知版本在写入前拒绝。
- 历史角色恢复支持旧 request/header 和新版 system/message；新增回归用例覆盖切人后历史归属。

## 尚未完成与支持边界

- 重启后的历史聊天头像、角色状态和游戏记录最终 UI 验收被中断，不计为通过。当前按候选版提供。
- 空白环境未配置模型；真实模型验收使用已有模型配置的本机环境。插件不提供模型账号或密钥。
- Linux/macOS 桌面及未来 DSH 版本未实机验收。三平台 CI 只验证包级构建与测试，不代表所有桌面环境可用。
- 安装器支持失败回退；断电、磁盘损坏、权限错误或并发安装不属于已验证恢复场景。保留备份，按 INSTALL.md 操作。
- 聊天装饰仍使用语义祖先 [data-phase] 定位，宿主升级后需重新验收。安装器拒绝未知版本不等于已安装插件能阻止宿主自动升级。
- 原生侧栏保持宿主默认会话列表；工作台另提供历史选择器。

GitHub Actions 的实际状态请查看仓库 Actions 页面。不能将本候选版描述为所有人、所有版本都不会出问题。